use vitcampus;

show tables from vitcampus;

select * from worker;

select first_name,department,salary from worker
where department='admin' or salary >= 110000;

select * from worker;

select first_name, department, salary from worker
where worker_id=1 or worker_id=7;

select worker_id, first_name, department, salary from worker
where worker_id in (1,6,7);

select worker_id, first_name, department, salary from worker
where worker_id not in (1,6,7);

select * from worker;

select * from worker
where salary between 100000 and 300000;

select * from worker
where salary between 110000 and 200000
and department not in ('hr', 'admin');

select first_name, department, salary from worker
where salary between 110000 and 300000
or department in ('hr', 'admin');


select * from worker where first_name LIKE '__i%';

select * from worker where first_name LIKE 'V%e_';

select * from worker where first_name LIKE '_i__%';

select * from worker where first_name LIKE '_i__a%';


select min(salary) from worker;

select max(salary) from worker;

select sum(salary) from worker where department='admin';

select avg(salary) from worker where department='hr';

select count(worker_id) from worker;

select min(salary) from worker
where department ="admin"
and first_name not like '%a%';

select * from worker 
where department = 'Admin'
and first_name not like 'a%'
and salary = (select MIN(salary) from worker where department = 'Admin');


select salary from worker
union
select salary from worker;

select salary from worker
union all
select salary from worker;


select worker_id as RollNumber from worker;

select distinct(department) from worker;

select worker_id, first_name,
case
	when salary > 300000 then 'Rich'
	when salary > 100000 and salary < 299999 then 'Middle'
	else 'Poor'
end
as status
from worker;


select min(salary) from worker where department='hr';
select max(salary) from worker where department='account';

create view minSalaryHr as select min(salary) from worker where department='hr';
create view maxSalaryAcc as select max(salary) from worker where department='account';
